name: election

on:
  push:

jobs:
  laughter:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        lever: ["on", "characteristic", "depressed", "process", "crack"]
        democracy: ["dumb", "flush", "personnel", "theoretical"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar Rd.jar
